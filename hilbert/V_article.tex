\documentclass[12pt]{article}

% Minimal & safe preamble
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{amsmath,amssymb,mathtools}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

% Load Rosetta
\input{V_macros.tex}

% Version tag
\newcommand{\VVersion}{v1.1}

\title{V: Reader’s Guide \& Contract Layer (\VVersion)\\
\small thermodynamic \texorpdfstring{\GOneStatement}{Delta S >= 0},\;
kinematic \texorpdfstring{\GTwoStatement}{U^(4)=0},\;
reciprocity \texorpdfstring{\GTwoWeakForm}{B(U,phi)=0},\;
conservation \texorpdfstring{\ConcludeDivT}{div T = 0}}
\author{Verification Tensor (V)}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
This note declares the shared \emph{contracts} (G1–G5) and an index-free \emph{Numerics Rosetta}
for matrix-free Newton–Krylov with a multigrid preconditioner. All handles are title-safe
(\verb|\ensuremath|) and may be used in text, headings, and captions.
\end{abstract}

\section*{Contracts (G1–G5)}
\begin{itemize}
  \item \textbf{G1 thermodynamic closure:} \GOneStatement.
  \item \textbf{G2 kinematic closure (strong):} \GTwoStatement\; i.e.\ \StrongForm.
  \item \textbf{G2 weak/reciprocity:} \GTwoWeakForm\; i.e.\ \WeakForm; summarized by \GThreeStatement.
  \item \textbf{G4 conservation (index-free):} \(\Div{\Current}=0\); translational case via \ConcludeDivT.
  \item \textbf{G5 statistical invariants:} \GFourDPI \; and \; \GFourScoreZero.
\end{itemize}

\section*{Shared symbols (continuum + statistics)}
\[
  \U,\ \V,\ \ph\in\Test,\ \grad,\ \C,\ \Bform,\ \Entropy,\ \DFour,\
  \Xi,\ \NoetherTensor{L}{\U}{\Xi},\ \Current,\ \StressEnergy,\ \Div{\Current},\
  \E,\ \Var{\cdot},\ \Estimator{\cdot},\ \Score,\ \Fisher,\ \KL{P}{Q},\ \Invariant{\cdot},\ \Res.
\]

\section*{Numerics Rosetta (JFNK + MG, index-free)}
Abstract Newton–Krylov with a multigrid preconditioner is referenced via:
\[
  \Rfunc(\U) := \DFour,\qquad
  \Jfunc(\U)\,v \approx \frac{\Rfunc(\U+\varepsilon v)-\Rfunc(\U)}{\varepsilon},\qquad
  \delta\U \ \text{from}\ \Krylov\big[\Jfunc,\ \MGVcycle\big].
\]
Discrete alignment (G3) supplies the hierarchy:
\[
  \DeltaTwoh,\ \DeltaFourh,\ \Bformh,\ \Restrict,\ \Prolong,\ \Smoother,\ \MGVcycle,\ \mesh.
\]
(Handles only; no stencil/indices are fixed in V.)

\section*{Usage notes (do \& don’t)}
\begin{itemize}
  \item Use statement handles directly in text: “By (\GTwoStatement) and (\GTwoWeakForm) …”
  \item Avoid \verb|\left...\right| unless both sides are present.
  \item Do not redeclare V handles in modules; add local aliases only when necessary.
\end{itemize}

\section*{Smoke test (text-mode safety)}
In text: \WeakForm\ \(\Rightarrow\) IBP \(\times 2\) \(\Rightarrow\) \StrongForm; solve via \NewtonStep\ with \MGPrecond; conclude \ConcludeDivT.

\end{document}
